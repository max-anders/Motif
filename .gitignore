# Build
/target/

# Local Cursor agent cockpit (private workflow — not the public contract)
.cursor/

# Personal sketch / app prefs saved by the app (cwd)
/project.json
/*.motif
/settings.json
/plugin_cache.json

# Secrets / local env (future-proof)
.env
.env.*
!.env.example
*.pem

# Editor / OS noise
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db
